# Downloader Local PDSI - Dependencias Python
# Instalacao: pip install -r requirements.txt

# Download de videos do YouTube
yt-dlp>=2024.1.0

# Processamento de audio
pydub>=0.25.1

# Metadados e dados
pandas>=2.0.0

# NOTA: Dependencias de sistema (nao instalaveis via pip):
# - SOX (obrigatorio): sudo apt-get install sox libsox-fmt-all
# - ffmpeg (obrigatorio): sudo apt-get install ffmpeg
